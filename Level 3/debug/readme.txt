
- exporrt TF_LOG_FILE= TRACE shows all the log traces to be following when trying to debug the code.
export TF_LOG_FILE_PATH= <path>.  executes the traces within that file

example for gitbash
Setup the trace.log file first

    export TF_LOG=TRACE
    export TF_LOG_PATH="trace.log"
    then IPA


OR OPTION 2 (didn't go the first time)

export TF_LOG_FILE_PATH=~/Desktop/Evythng/Cloud\ Projects\ Done/Terraform-main/4.\ Variables\ with\ Default\ Values/trace.log

