- This is relating more to how we properly handle identification of resources created,
- Who created what resource at what time and how can we get in touch with these persons.

- The local variables are defined at the top of the file and recalled where they are mentioned in the file.

- once defined they are recalled using the 'merge(local.<variable_name>,{Name = "<Name>"})'

    Would have to try mapping it from a completely different variable file
    And defining the name tags already in the tag section of the file.


- This first example of it is much more efficient and practical as compared to the method which is stated in example 2